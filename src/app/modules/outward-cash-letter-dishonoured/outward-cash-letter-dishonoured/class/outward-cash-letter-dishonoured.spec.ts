import { OutwardCashLetterDishonoured } from './outward-cash-letter-dishonoured';

describe('OutwardCashLetterDishonoured', () => {
  it('should create an instance', () => {
    expect(new OutwardCashLetterDishonoured()).toBeTruthy();
  });
});
